apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-aoa-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guestbook-aoa-backend
  template:
    metadata:
      labels:
        app: guestbook-aoa-backend
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-aoa-backend
        ports:
        - containerPort: 80
