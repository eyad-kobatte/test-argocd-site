apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-aoa-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guestbook-aoa-ui
  template:
    metadata:
      labels:
        app: guestbook-aoa-ui
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-aoa-ui
        ports:
        - containerPort: 80
